
"""
(二叉树-路径求和) 给定一个二叉树和一个目标整数 targetSum，找出所有从根节点到叶子节点的路径，要求路径上节点值的总和等于 targetSum。返回这些路径的列表 。
"""


"""
(二分查找-应用) 你需要将一批货物通过传送带运送，必须在 D 天内运完。货物列表 weights 给出了每件货物的重量，传送带每天可以运送无限件货物，但有最大载重上限。货物必须按 weights 列表给定的顺序运输，不能打乱。请求出传送带的最低有效载重 W，使得所有货物能在 D 天内运完 。
"""


"""
练习题目4: 旅客满意度逻辑回归（机器学习，20分）
题目描述：给定旅客数据集（CSV，特征：年龄、航班延误，标签：满意/不满意），实现数据预处理（缺失值均值填充、标准化），训练逻辑回归模型，预测测试集满意度。输出准确率、ROC曲线（AUC值）。比较不同学习率的效果。
考察方向：数据预处理与模型训练（大纲指定逻辑回归/评估指标），模拟旅客分类。
练习重点:

用pandas/sklearn处理数据（SimpleImputer, StandardScaler）。
训练LogisticRegression，计算roc_auc_score。
调参（学习率C=0.1, 1, 10）。
练习混淆矩阵和ROC绘制。
"""




"""
使用PyTorch或TensorFlow，构建一个符合以下描述的CNN模型 ：

输入是一个 3x32x32 的图像张量（3个通道，高宽为32）。

第一层：一个卷积层，32个 3x3 的卷积核，步长为1，padding为1。

一个ReLU激活函数。

第二层：一个 2x2 的最大池化层。

一个全连接层，输出为10个类别。

任务：依次打印出数据张量在通过 卷积层后 和 池化层后 的形状（shape），考察对 feature map 尺寸变化的理解 
"""

